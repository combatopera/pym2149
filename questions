buffering:

qtonpbuf period buffering for tone
qnoipbuf period buffering for noise
qenvpbuf period buffering for env
         mixer off buffering for tone
         mixer off buffering for noise
qenvrbuf envelope reset buffering
         fixed level change buffering

edge values:

qtonpzer effect of period 0 on tone
qnoispec effect of period 0 on noise
qenvpzer effect of period 0 on envelope

calibration:

         fixed level to amp mapping
         variable level to amp mapping
qanlgmix analog mix function
         tone initial value
         noise initial value

algos:

qnoispec noise period spectrum
qnoispec confirm noise lfsr
qanlgmix mixer off with fixed level
qenvpbuf mixer off with variable level
qbmixenv binary mixing function effect on env

resets:

         mixer on reset for tone
         mixer on reset for noise
